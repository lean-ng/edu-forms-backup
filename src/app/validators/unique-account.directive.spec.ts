import { UniqueAccountDirective } from './unique-account.directive';

describe('UniqueAccountDirective', () => {
  it('should create an instance', () => {
    const directive = new UniqueAccountDirective();
    expect(directive).toBeTruthy();
  });
});
