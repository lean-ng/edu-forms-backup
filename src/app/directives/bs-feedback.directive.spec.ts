import { BsFeedbackDirective } from './bs-feedback.directive';

describe('BsFeedbackDirective', () => {
  it('should create an instance', () => {
    const directive = new BsFeedbackDirective();
    expect(directive).toBeTruthy();
  });
});
